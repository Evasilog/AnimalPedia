<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animalpedia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalPedia">
        <activity android:name=".AnimalView" />
        <activity android:name=".Settings" android:theme="@style/Theme.AnimalPedia"/>
        <activity android:name=".ContinentView" />
        <activity android:name=".CategoryView" />
        <activity android:name=".Map" />
        <activity android:name=".Favorites" />
        <activity android:name=".Search" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>